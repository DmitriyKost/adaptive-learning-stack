# GPU override for docker-compose.with-adaptive-sql-adaptive_sql_diploma.yml.
# Requires NVIDIA Container Toolkit on the host.

services:
  intelligence:
    environment:
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
    gpus: all
